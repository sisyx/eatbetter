type Package = {
    name: String,
    currency: String,
    maxDiet: Number,
    price: Number
};

type PackageProps = {
    name: String,
    currency: String,
    maxDiet: Number,
    price: Number
};

export type { Package, PackageProps}